export interface ICountry {
  name: string;
}

export interface ICartCardData {
  id: string;
  name: string;
  initialVolume: string;
  increaseVolume: string;
  unit: string;
  price: string;
  currency: string;
  imgPath: string;
}
